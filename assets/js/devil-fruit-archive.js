/**
 * Devil Fruit Archive — JS di frontend.
 *
 * L'interazione delle card (hover/lift) e i badge sono gestiti
 * interamente via CSS: qui non serve logica applicativa. Il file resta
 * come punto di innesto per eventuali micro-interazioni future, senza
 * dipendenze esterne.
 */
( function () {
	'use strict';

	document.documentElement.classList.add( 'dfa-js-ready' );
} )();
