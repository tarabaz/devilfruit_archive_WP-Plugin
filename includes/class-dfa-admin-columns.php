<?php
/**
 * Colonne personalizzate nella lista admin degli esemplari.
 *
 * TODO (step 2): colonne "Catalog ID" e "Type", ordinabili.
 *
 * @package DevilFruitArchive
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

class DFA_Admin_Columns {

	/**
	 * Aggancia i filtri per le colonne della lista admin.
	 */
	public static function init() {
		// Implementato nello step 2 (manage_esemplare_posts_columns,
		// manage_esemplare_posts_custom_column, sortable columns).
	}
}
